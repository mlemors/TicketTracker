<UserControl x:Class="TicketTracker.Views.EmptyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <!-- Empty State Content -->
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="40">

            <!-- Large Ticket Icon -->
            <TextBlock Text="🎫"
                       FontSize="80"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       Opacity="0.5"
                       Margin="0,0,0,30" />

            <!-- Main Message -->
            <TextBlock Text="Keine Tickets vorhanden"
                       FontSize="24"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       Margin="0,0,0,15" />

            <!-- Instructions -->
            <TextBlock Text="Erstellen Sie ein Ticket, um mit der Zeiterfassung zu beginnen."
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       Opacity="0.7"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,30"
                       MaxWidth="400" />

            <!-- Create Ticket Button -->
            <Button Width="200" Height="45"
                    FontSize="14" FontWeight="SemiBold"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Click="CreateTicketButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource NavigationHoverBrush}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="+"
                               FontSize="16" FontWeight="Bold"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center" />
                    <TextBlock Text="Erstes Ticket erstellen"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>